apiVersion: v1
kind: Service
metadata:
  name: ParentInfoAppService
spec:
  type: ClusterIP
  
  selector:
    app: ParentInfoApp

  ports:
    - name: http-port-mapping
      port: 80 
      targetPort: 8000 
      protocol: TCP
